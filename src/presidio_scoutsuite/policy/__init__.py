"""Curated ScoutSuite rulesets shipped with presidio-hardened-scoutsuite.

This is a package (not just a data dir) so the JSON baselines load reliably via
``importlib.resources`` from a wheel/zip install, not only an editable checkout.
"""
